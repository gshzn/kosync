---
import { Button } from '@/components/ui/button';

const appUrl = import.meta.env.PUBLIC_APP_URL;
---

<section class="bg-primary py-20 text-primary-foreground">
  <div class="container mx-auto px-4 text-center">
    <h2 class="mb-4 text-3xl font-bold tracking-tight sm:text-4xl">
      Ready to start syncing?
    </h2>
    <p class="mx-auto mb-8 max-w-2xl text-lg text-primary-foreground/90">
      Stop hassling with connecting your Kobo e-reader to your PC for every new book you want to read. Instead, use KoSync.
    </p>
    <Button size="lg" variant="secondary" className="w-full sm:w-auto" href={appUrl}>
      Get Started
    </Button>
  </div>
</section>

